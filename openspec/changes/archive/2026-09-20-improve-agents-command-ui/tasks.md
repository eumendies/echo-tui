## 1. Surface 信息契约

- [x] 1.1 扩展 `AgentsCommandSurface` 与相关 row 类型，加入结构化选中项摘要、范围统计、分区和动作语气字段，并按项目规范为新增结构字段补充中文领域注释。
- [x] 1.2 在 `AgentsCommandHandler` 的 surface 投影中生成中文 Tab 标签、短状态语义、范围统计，以及 Agent、诊断和动作行各自对应的摘要，保持现有 rows、selectedIndex 与事件状态机不变。
- [x] 1.3 为内置详情、自定义详情、创建/编辑表单和内置策略表单补充分区元数据与危险动作语气，确认分区不进入可聚焦行序列。

## 2. 响应式列表与摘要渲染

- [x] 2.1 重构 Agent 列表行，只展示名称、状态、来源与 capability，并加入不依赖颜色的状态文本、未知状态回退和顶部范围统计。
- [x] 2.2 实现选中项摘要渲染，覆盖有效 Agent、shadowed/invalid/reserved 物理项、普通诊断和新建动作，确保切换焦点时不会残留上一项信息。
- [x] 2.3 实现宽终端左右分栏和窄终端上下堆叠布局，集中定义断点，并使用终端可见宽度计算保证长文本、宽字符和控制字符安全。
- [x] 2.4 调整列表窗口与 `maxLines` 预算，在宽窄布局和极低高度下优先保留当前选中行、核心摘要、更多提示及 footer 提示。

## 3. 详情、表单与编辑态渲染

- [x] 3.1 在详情和表单中渲染“身份”“运行策略”“能力与权限”“操作”等分区，并将保存、取消、配置和危险操作与普通字段区分。
- [x] 3.2 让分区标题参与显示行预算但不改变 selectedIndex，确保受限高度中的当前字段、滚动提示和动作始终与 controller 焦点一致。
- [x] 3.3 校准 name、description 与 instructions 编辑态的真实终端光标，在 resize、分区窗口和窄终端裁剪后仍落在对应文本位置。
- [x] 3.4 在宽度约定兼容时复用配置面板行原语；如需调整公共原语，同步验证 `/config` 与 `/mcp` 的现有渲染行为。

## 4. 自动化测试与文档

- [x] 4.1 更新 command handler 测试，覆盖摘要、统计、中文范围标签、section 元数据、动作摘要和未知/异常状态，同时保留现有 Tab、Enter、Esc、确认、冲突及草稿行为断言。
- [x] 4.2 更新 renderer 测试，覆盖 50、80、96、120 列布局、宽窄切换、有限高度、长文本、宽字符、控制字符、状态语义和当前选中行可见性。
- [x] 4.3 增加详情与表单分区、危险动作、窗口滚动以及行内和多行编辑真实光标的渲染测试，避免整屏 ANSI 快照断言。
- [x] 4.4 更新 `docs/tui-architecture.md` 中 `/agents` surface 的信息层级、响应式布局和分区投影说明。

## 5. 验证

- [x] 5.1 运行 `npm run typecheck`。
- [x] 5.2 运行 `npm test`。
- [x] 5.3 运行 `find bin src test scripts -name '*.js' -exec node --check {} \;`。
- [x] 5.4 使用 `npm start` 手动验证 `/agents` 在 50、80、96、120 列及 resize 下的总览、来源 Tab、详情、表单、Tools、Skills、instructions、确认、错误反馈和 Esc 返回层级。
